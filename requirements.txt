# ApexMind runtime dependencies (Python 3.11). Install: pip install -e .
pandas>=2.0
openpyxl>=3.1
requests>=2.31
ddgs>=9.0
pyyaml>=6.0
tenacity>=8.2
tqdm>=4.66
# dev / extras
pytest>=8.0
python-pptx>=0.6
